def run() -> dict:
    """
    Implement the approach described in prompt.md inside the current world.

    Read world.json to discover the environment yin created.
    Read yin.py and law.md when useful so you understand the active critique.
    Measure everything that could be relevant.
    Return a JSON object. Nested dicts and lists are allowed.
    Put large traces in artifact files and return their paths.
    Do not raise; catch exceptions and return them as keys.

    Keys should be self-describing, for example:
        "summary", "evidence", "artifacts"

    Yin decides what counts as a pass.
    Measure more than you think you need. Yin can always start using it later.
    """
    results = {}

    # --- yang implementation here ---

    return results
